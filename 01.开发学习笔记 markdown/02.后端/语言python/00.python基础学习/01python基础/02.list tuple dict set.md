---
title: list tuple dict set类型
categories:
  - 01.开发学习笔记 markdown
  - 05.语言python
  - 00.python基础学习
  - 01python基础
tags:
  - python
date:
---

``` python

# list 
# 数组 相当于js里的array
classmates = ['Michael', 'Bob', 'Tracy']

# tuple 有序列表元组 和list非常相似，但是初始化后不能修改
mates = ('Michael', 'Bob', 'Tracy')

# dict 
# 字典 在其他语言中也称为map，使用键-值（key-value）存储，具有极快的查找速度
# 给定一个名字，比如'Michael'，dict在内部就可以直接计算出Michael对应的存放成绩的“页码”，也就是95这个数字存放的内存地址，直接取出来，所以速度非常快
# 由于一个key只能对应一个value，所以，多次对一个key放入value，后面的值会把前面的值冲掉
d = {'Michael': 95, 'Bob': 75, 'Tracy': 85}

# set
# set和dict类似，也是一组key的集合，但不存储value。由于key不能重复，所以，在set中，没有重复的key
# set可以看成数学意义上的无序和无重复元素的集合，因此，两个set可以做数学意义上的交集、并集等操作
s = set([1, 2, 3])
print(s)

```

